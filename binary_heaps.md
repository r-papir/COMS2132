# Binary Heaps


A collection Q of comparable elements that
supports the following operations:
• insert(x): add an element to Q (compare to
enqueue)
• deleteMin(): return the minimum element in Q
and delete it from Q (compare to dequeue).


to be continued...
